# +one — crawler rules for https://www.plusoneco.in
# Public marketing/legal pages (/ , /privacy, /terms, /support) are indexable.
# The app shell under /app is auth-gated, so crawlers are kept out of it.
# /api/* is proxied to the backend and is not for crawlers.

User-agent: *
Allow: /
Disallow: /app/
Disallow: /api/

Sitemap: https://www.plusoneco.in/sitemap.xml
